import React from 'react';
import Header from '../components/Header';

const Drawit = () => {
    return (
        <div>
            <Header />
            <h1>Start to create!</h1>
        </div>
    );
};

export default Drawit;